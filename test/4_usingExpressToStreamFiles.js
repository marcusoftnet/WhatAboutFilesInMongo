var should = require("should");
var utils = require("../utils.js");
var config = require('../config.js').config;
var fsAccess = require("../usingMongooseGridFs.js");

describe("This is nice and everything, but how about move to the 21th century and use this on the web", function () {
	it("well, it's not hard at all to stream data back");
	it("it's pretty easy to upload a file to the grid too");
});